version: 2.1
jobs:
  test:
    docker:
      - image: alpine:3.20
    steps:
      - checkout
      - run:
          name: test
          command: |
            test "$CIRCLECI" = "true"
            test -f hello.txt
            echo "circleci-example-ok"
workflows:
  main:
    jobs: [test]
