name: CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: test
        run: |
          test "$GITHUB_ACTIONS" = "true"
          test -f hello.txt
          echo "github-example-ok"
