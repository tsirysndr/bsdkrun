[mariadbd]
datadir                     = /var/lib/mysql
socket                      = /tmp/mysqld.sock
pid-file                    = /tmp/mysqld.pid
bind-address                = 0.0.0.0
port                        = 3306

# Unikraft constraints
#
# The guest has neither io_uring nor io_submit(), so InnoDB's native AIO
# cannot be used; without this mariadbd fails at startup rather than
# degrading. (liburing.so is still in the image -- mariadbd links it -- it
# just must never be asked to set a ring up.)
innodb_use_native_aio       = 0
# lwip is built without DNS (CONFIG_LWIP_DNS=n), so a reverse lookup of every
# connecting client would block until it times out. Nothing in this image
# reads /etc/hosts either.
skip_name_resolve           = ON
# One thread fewer, and nothing here schedules events.
event_scheduler             = OFF
# The transaction coordinator, and the reason this is here rather than in a
# "features" section. With two 2PC-capable engines (InnoDB and Aria) and no
# binary log, MariaDB coordinates commits through tc.log, which it maps
# MAP_SHARED -- and Unikraft has no shared file mappings:
#
#   mmap(addr=0, len=24576, prot=3, flags=1, fd=21, offset=0) failed: -95
#   [ERROR] Can't init tc log
#   [ERROR] Aborting
#
# With the binary log on, the binlog *is* the coordinator (TC_LOG_BINLOG) and
# nothing is mapped. The log is sequential writes into the same ramfs as the
# datadir, and expires with it.
log_bin                     = /var/lib/mysql/binlog
expire_logs_days            = 1
max_binlog_size             = 16M
server_id                   = 1

# Memory
#
# The whole image is resident twice at boot -- embedded in the kernel and
# again unpacked into ramfs -- so keep the server's own appetite small. This
# is a demo database, not a write-heavy one.
performance_schema          = OFF
innodb_buffer_pool_size     = 64M
innodb_log_file_size        = 16M
max_connections             = 32
table_open_cache            = 400

# TLS off, and this one is not a preference -- it is the guest clock.
#
# MariaDB 11.4 has automatic TLS: with no certificate configured it mints a
# self-signed one at *startup* rather than shipping one in the image. The
# guest has no RTC and boots at the epoch, so the certificate it writes is
# valid from 1970 to 1971, and every client rejects the connection out of
# hand:
#
#   ERROR 2026 (HY000): TLS/SSL error: certificate has expired
#
# The server is fine -- the handshake got far enough to negotiate TLS at all
# -- but no client can get past it without being told to skip TLS. An empty
# tls_version disables it, the same spelling ../unikraft-mysql uses.
#
# It would be wrong to fix this by baking a certificate at image build time
# instead: everyone who pulled the image would share one private key.
# Terminate TLS in front of the guest.
tls_version                 =
