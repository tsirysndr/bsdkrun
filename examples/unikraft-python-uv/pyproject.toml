[project]
name = "server"
version = "0.1.0"
description = "A uv-managed Python service running as a Unikraft unikernel"
requires-python = ">=3.12"
dependencies = [
    "bottle>=0.13",
]

[tool.uv]
package = false
