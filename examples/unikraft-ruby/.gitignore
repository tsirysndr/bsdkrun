/node_modules
/.unikraft/
.config*
.Kraftfile.*
/.rootfs-*/

# Rust toolchain cache for the containerised build (see build.sh)
.rust-cache/

# Build and run transcripts, kept out of the tree like every other log here.
*.log

# Generated by kraft during the build (transient).
Makefile.uk
