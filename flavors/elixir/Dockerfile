# Generated by `bsdkrun flavor __dockerfiles` — do not edit.
#
# Flavor: elixir (language)
# Elixir/Erlang (mix, hex).
#
# Published as ghcr.io/tsirysndr/bsdkrun-flavor-elixir:latest and pulled by `bsdkrun flavor run elixir`,
# so a first launch does not have to provision a VM.
FROM elixir:1.17

# bsdkrun runs a guest as root with no service manager, and provisions
# in one shell per step — the same shape as these RUN layers.
SHELL ["/bin/sh", "-c"]
RUN mix local.hex --force >/dev/null 2>&1 || true

RUN mix local.rebar --force >/dev/null 2>&1 || true

