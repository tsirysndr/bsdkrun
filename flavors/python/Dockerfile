# Generated by `bsdkrun flavor __dockerfiles` — do not edit.
#
# Flavor: python (language)
# Python 3.12 with the uv package manager.
#
# Published as ghcr.io/tsirysndr/bsdkrun-flavor-python:latest and pulled by `bsdkrun flavor run python`,
# so a first launch does not have to provision a VM.
FROM python:3.12-slim

# bsdkrun runs a guest as root with no service manager, and provisions
# in one shell per step — the same shape as these RUN layers.
SHELL ["/bin/sh", "-c"]
RUN pip install --no-cache-dir uv 2>/dev/null || (apt-get update && apt-get install -y curl && curl -LsSf https://astral.sh/uv/install.sh | sh)

